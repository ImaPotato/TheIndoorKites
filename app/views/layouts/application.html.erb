<!DOCTYPE html>
<html>
<head>
  <title>TheIndoorKites</title>
  <%= stylesheet_link_tag    "application", media: "all", "data-turbolinks-track" => true %>
  <%= javascript_include_tag "application", "data-turbolinks-track" => true %>
  <%= csrf_meta_tags %>
</head>
<body>
<div  style = "padding-bottom:50px" style = "width:100%"> <!-- don't forgot to remove this -->
    <div id="header" class="pure-menu pure-menu-open pure-menu-horizontal">
        <%= link_to 'Kelburn Postal Service', homepage_path, class: "pure-menu-heading" %>
        <ul>
            <li>
                <a href="#">Mail</a>
            </li>
            <li>
                <a href="#">Transport firms</a>
            </li>
            <li>
                <a href="#">Connections</a>
            </li>
            <li>
                <a href="#">Pricing</a>
            </li>
          </ul>
      <ul style = "float:right"> <!-- this needs to be removed as well; in-line is bad. -->
            <% if current_user %>
                <li>
                    <%= link_to 'Log out', log_out_path%>
                </li>
            <% else %>
                <li>
                    <%= link_to 'Sign up', sign_up_path%>
                </li>
                <li>
                    <%= link_to 'Log in', log_in_path %>
                </li>
            <% end %>
        </ul>
    </div>
</div>

<% flash.each do |name, msg| %>
<%= content_tag :div, msg, :id => "flash_#{name}"%>
<% end %>
<%= yield %>

</body>
</html>
